using ShoppingCart.Controllers;
using Xunit;

namespace ShoppingTest
{
    public class ShoppingCartServiceTests
    {
        internal  ShoppingCartServiceTests()
        {

        }

        //[Fact]
        //[Theory(DisplayName = "Add Item To Cart")]
        //[ClassData(typeof(AddItemTestDataWithId))]
        //public async Task AddItemToCart_ShouldItemNotFound_WhenIdIsNotInItemDatabaseAsync(AddItemTestParameters parameters)
        //{
        //    ProductsController  service = new ProductsController(_context);
        //    var result = await Record.ExceptionAsync(() => service.AddItemToCart(parameters.TestItemDto, parameters.TestShoppingCart));
        //    var exception = Assert.IsType<ItemNotFoundException>(result);
        //}


    }
}